---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: providerconfigs.nutanix.crossplane.io
spec:
  group: nutanix.crossplane.io
  names:
    categories:
      - crossplane
      - provider
      - nutanix
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
        - jsonPath: .spec.credentials.secretRef.name
          name: SECRET
          priority: 1
          type: string
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: Nutanix provider configuration.
          properties:
            apiVersion:
              description: API version of this object.
              type: string
            kind:
              description: REST resource type.
              type: string
            metadata:
              type: object
            spec:
              description: Desired state of the provider config.
              properties:
                allowedLobs:
                  description: Allowed Line of Business values for VMs.
                  items:
                    type: string
                  type: array
                availabilityZoneMappingURL:
                  description: URL for AZ to cluster mapping CSV.
                  type: string
                credentials:
                  description: Provider credentials.
                  properties:
                    env:
                      description: Ref to env var with credentials.
                      properties:
                        name:
                          description: Env var name.
                          type: string
                      required:
                        - name
                      type: object
                    fs:
                      description: Path to credentials file.
                      properties:
                        path:
                          description: Filesystem path.
                          type: string
                      required:
                        - path
                      type: object
                    secretRef:
                      description: Secret with provider credentials.
                      properties:
                        key:
                          description: Secret key.
                          type: string
                        name:
                          description: Secret name.
                          type: string
                        namespace:
                          description: Secret namespace.
                          type: string
                      required:
                        - key
                        - name
                        - namespace
                      type: object
                    source:
                      description: Source type for credentials.
                      enum:
                        - None
                        - Secret
                        - InjectedIdentity
                        - Environment
                        - Filesystem
                      type: string
                  required:
                    - source
                  type: object
                datacenterCredentials:
                  additionalProperties:
                    description: Datacenter-specific credentials.
                    properties:
                      env:
                        description: Env var reference.
                        properties:
                          name:
                            description: Env var name.
                            type: string
                        required:
                          - name
                        type: object
                      fs:
                        description: Filesystem path reference.
                        properties:
                          path:
                            description: Path.
                            type: string
                        required:
                          - path
                        type: object
                      secretRef:
                        description: Secret reference.
                        properties:
                          key:
                            description: Secret key.
                            type: string
                          name:
                            description: Secret name.
                            type: string
                          namespace:
                            description: Secret namespace.
                            type: string
                        required:
                          - key
                          - name
                          - namespace
                        type: object
                      source:
                        description: Source type for credentials.
                        enum:
                          - None
                          - Secret
                          - InjectedIdentity
                          - Environment
                          - Filesystem
                        type: string
                    required:
                      - source
                    type: object
                  description: Datacenter credentials mapping.
                  type: object
                enableAvailabilityZoneMapping:
                  description: Enable AZ mapping.
                  type: boolean
                isLobMandatory:
                  description: Whether LoB is mandatory.
                  type: boolean
                prismCentralEndpoints:
                  additionalProperties:
                    type: string
                  description: Datacenter to Prism Central endpoints mapping.
                  type: object
              required:
                - credentials
              type: object
            status:
              description: Observed state of the provider config.
              properties:
                conditions:
                  description: Resource conditions.
                  items:
                    description: Condition details.
                    properties:
                      lastTransitionTime:
                        description: Last change time.
                        format: date-time
                        type: string
                      message:
                        description: Condition message.
                        type: string
                      observedGeneration:
                        description: Generation observed.
                        format: int64
                        type: integer
                      reason:
                        description: Reason for last transition.
                        type: string
                      status:
                        description: Current status (True, False, Unknown).
                        type: string
                      type:
                        description: Condition type.
                        type: string
                    required:
                      - lastTransitionTime
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                users:
                  description: Number of users using this config.
                  format: int64
                  type: integer
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources: {}
